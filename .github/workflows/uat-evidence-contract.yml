name: Release 1 UAT Evidence Contract

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

env:
  CAREPOINT_R7_SOURCE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}

jobs:
  contract:
    name: R7 UAT evidence contract
    runs-on: ubuntu-24.04
    steps:
      - name: Check out exact candidate SHA
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
          ref: ${{ env.CAREPOINT_R7_SOURCE_SHA }}
      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22"
      - name: Verify exact checkout identity
        run: test "$(git rev-parse HEAD)" = "$CAREPOINT_R7_SOURCE_SHA"
      - name: Self-test fail-closed UAT evidence contract
        run: node .ci/release-uat-evidence-contract.mjs --self-test
      - name: Verify non-approved UAT evidence template
        run: >-
          node .ci/release-uat-evidence-contract.mjs
          --validate-template ops/release-1/uat-evidence.example.json
      - name: Capture sanitized UAT contract evidence
        run: |
          mkdir -p uat-contract-evidence
          node .ci/release-uat-evidence-contract.mjs \
            --contract-evidence \
            ops/release-1/uat-evidence.example.json \
            uat-contract-evidence/contract-evidence.json
          sha256sum \
            .ci/release-uat-evidence-contract.mjs \
            ops/release-1/uat-evidence.example.json \
            uat-contract-evidence/contract-evidence.json \
            > uat-contract-evidence/SHA256SUMS
          printf 'sourceSha=%s\nproductionAcceptance=false\nworkflowPurpose=contract-validation-only\nhumanUatExecuted=false\nsyntheticDataOnly=true\n' \
            "$CAREPOINT_R7_SOURCE_SHA" \
            > uat-contract-evidence/metadata.txt
      - name: Upload R7 UAT contract evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: carepoint-uat-contract-${{ env.CAREPOINT_R7_SOURCE_SHA }}
          path: uat-contract-evidence
          if-no-files-found: error
